// Redis Pub/Sub for real-time updates - placeholder
export const redis = {
  subscribe: async (_channel: string, _handler: (msg: string) => void) => {},
  publish: async (_channel: string, _msg: string) => {},
};
