// Placeholder for Dio HTTP client - implement when API is ready
// import 'package:dio/dio.dart';

class DioClient {
  static const String baseUrl = 'http://localhost:3000';
  // Dio getInstance() => Dio(BaseOptions(baseUrl: baseUrl));
}
